The package is for Bayesian Model Averaging in non-Gaussian error regression models, mainly stochastic frontier analysis. The package uses paralelization toolbox, so it is better to have it for speed.

Main functions:
sfa_fit_rep - calculates a single model and returns its results structure
